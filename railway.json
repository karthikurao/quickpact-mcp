{
  "build": {
    "commands": [
      "pip install --upgrade pip",
      "pip install -r requirements.txt"
    ]
  },
  "deploy": {
    "cmd": "python quickpact_mcp_server.py",
    "healthcheckPath": "/mcp",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
